import styled from "styled-components";
import { uiKitModules } from "../constants";

export const StyledContainer = styled.div`
  width: 100%;
  max-width: 1040px;
  margin: 0 auto;
  padding: 0 ${uiKitModules.x5}px;
`;
